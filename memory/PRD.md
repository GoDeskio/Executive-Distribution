# Executive Distribution — PRD

## Original Problem Statement
Build a fully dynamic, professional website "Executive Distribution" (Import/Export & product sourcing company): Hero home page; program/service cards (image on top, title, short description) linking to full description pages controlled/edited from an admin dashboard; full admin dashboard with SEO controls for the whole site, a functional CRM (clients, visitors, documents), a left sidebar (settings, profile, object storage for assets & documents); a visitor activity heatmap monitored in admin. Dark masculine steel-blue palette, expensive look; logos editable from admin.

## User Choices
- Auth: JWT email + password
- Heatmap: click heatmap overlay + visitor analytics (both)
- Scope: full build (Services + SEO + CRM + assets)
- Images: mix of real stock + curated
- Accent: steel blue / gunmetal

## Architecture
- Backend: FastAPI + MongoDB (motor). JWT (bcrypt) auth, Bearer token. Emergent object storage for assets/documents. Routes under /api.
- Frontend: React 19 + Tailwind + shadcn + framer-motion + recharts. Token in localStorage (`ed_token`).
- Design: `/app/design_guidelines.json` (Playfair Display + Manrope, obsidian + steel blue).

## Implemented (2026-06)
- **Auto-import + bulk tagging (2026-06):** Research form now has an "Auto-save contacts to CRM" toggle (with an optional Import tag field); the scrape endpoint accepts `auto_import`/`import_tag` and imports all found emails right after scraping, returning an `import_summary` (surfaced in the scrape toast). The Save-to-CRM picker gained a Tag input; `save-contacts` accepts a `tags[]` array applied to every imported/enriched lead alongside `research`. Import logic refactored into a shared `_import_contacts` helper. Verified via curl (custom tag applied, auto_import summary on scrape) + UI e2e (toggle reveals tag field, picker tag input renders).
- **CRM contact picker + lead dedupe (2026-06):** The Research "Save to CRM" button now opens a picker modal listing every extracted email (title/url/phone context) with checkboxes + Select all/Clear; `POST /api/research/{id}/save-contacts` accepts an optional `emails` subset. De-dupe is now enrichment, not skip: an existing lead gets its missing phone filled, the source note appended (if new), and the `research` tag added — counted as `updated`. Response returns `{created, updated, found}`; toast reports both. Verified via curl (subset import, new-vs-updated, enrichment of a phone-less lead) + UI e2e (picker renders, subset save → "CRM: 1 new lead").
- **Save Research contacts to CRM (2026-06):** New `POST /api/research/{id}/save-contacts` (crm perm) turns emails extracted by the scraper into CRM leads — company/name from the page title (fallback to email domain), first phone found, `status=lead`, `tags=["research"]`, and a note citing the source URL. De-dupes against existing clients by email (idempotent). Research page shows a "Save N to CRM" button next to "AI brief" whenever the current run has emails; toast reports created/skipped counts. Verified end-to-end via curl (2 created → re-run skips 2) + UI e2e (button renders, "Added 2 leads to CRM" toast).
- **ScraperAPI enabled + AI research brief (2026-06):** ScraperAPI key stored in settings (write-only), so the Research tab's "Render JavaScript" toggle now works (verified: engine=scraperapi on example.com; added retry/backoff for transient ScraperAPI 5xx). New `POST /api/research/{id}/summarize` feeds scraped content to the AI assistant and returns a structured sourcing brief (Overview, Key findings per site, Suppliers/products, Pricing, Contacts, Next steps), saved on the run. Frontend "AI brief / Regenerate" button + rendered brief panel. Verified via curl + UI e2e.
- **Research tab / web scraping (2026-06):** New admin **Research** page (gated by a new `research` permission). Admin enters keywords + a list of URLs (max 15); backend scrapes each page and extracts title, meta description, clean text, links, emails, phone numbers, and keyword matches with snippets. Free engine = `requests` + BeautifulSoup (`core/scraper.py`); optional **"Render JavaScript"** mode uses ScraperAPI when a `scraperapi_key` is set in Settings (write-only, `has_scraperapi_key` flag). Respects robots.txt (cached per host), retries/backoff, size caps. Results saved to a deletable Research history (`POST /api/research/scrape`, `GET /api/research`, `DELETE /api/research/{id}`). Verified 17/17 backend tests + UI e2e. ScraperAPI key deferred pending user key.
- **Slack alerts for scheduled backups (2026-06):** `core/notify.send_slack` (reuses the configured `slack_webhook_url`). When `alert_on_backup` is on, the scheduled-backup poller posts a success message (filename/size/retention) or a `⚠️` failure alert. Toggle in the Dashboard Backups panel (with a hint to set the webhook in Settings). `/api/backup/config` now returns `alert_on_backup` + `has_slack_webhook`. Verified via curl.
- **Scheduled automatic backups (2026-06):** background `_backup_poller` in server.py writes a server-folder backup on a configurable interval (`backup_schedule_interval_hours`, default 24h) when `backup_schedule_enabled` is on, then prunes to the newest `backup_retention` (default 7) via `prune_disk_backups`. Controls added to the Dashboard Backups panel (enable toggle, interval hours, keep-last count, last-run timestamp). Verified: config save/read + prune (4→keep 2→removed 2).
- **Backup & Restore (2026-06):** Superadmin Dashboard **Backups** panel. Endpoints (all superadmin): `GET /api/backup/config`, `GET /api/backup/download` (zip to computer), `POST /api/backup/save` (to server folder), `GET/DELETE /api/backup/server/{filename}`, `POST /api/backup/restore`. A backup = all core collections (users, services, settings, clients, quotes, documents, files, notifications, audit_logs) + optionally uploaded files, as a zip. Destination: download to computer AND/OR a configurable server folder (`backup_dir`, default `/app/backend/backups`). Restore is confirm-gated, **auto-creates a safety backup first**, and **rolls back to a snapshot if any step fails** (no partial wipes). Auto-backup runs right before an update is applied (`backup_auto_before_update`). Path-traversal guarded. Verified 28/28 backend tests + rollback/safety-backup curl checks. NOTE: S3 & Google Drive destinations were requested but need credentials — deferred pending keys.
- **Scheduled automatic update check (2026-06):** background poller in `server.py` (`_update_poller`) runs ~90s after startup then every 24h (env `UPDATE_CHECK_INTERVAL_SECONDS`). Pull/notify-only: checks GitHub, sets `update_available`, raises a notification, surfaces the AdminLayout banner without requiring a login. Auto-apply fires only when `update_auto_apply` is on AND `UPDATE_SCRIPT` exists (self-host). Apply logic centralized in `core/updater.run_update_script`.
- **Hardening (2026-06):** audit CSV export (`/api/audit/export.csv`) now streams from the DB cursor with `limit`(≤100k)/`skip` paging; settings PUT is whitelisted to `DEFAULT_SETTINGS` keys (drops unknown/internal fields like a spoofed `update_available`).
- **GitHub update checker/notifier (2026-06):** Superadmin Dashboard panel to configure a GitHub repo (owner/repo, branch, optional token — pre-filled with GoDeskio/Executive-Distribution). Endpoints `GET /api/updates/status` (auto-refreshes when stale), `POST /api/updates/check`, `/mark-current`, `/apply` (all superadmin). Detects newer release/commit, raises a notification + an "Update available" banner in AdminLayout. `apply` runs a self-host update script via `UPDATE_SCRIPT` env; on managed hosting it returns guidance to use platform Deploy. Verified 22/22 backend tests.
- **DATA PERSISTENCE THROUGH UPDATES (2026-06):** All data lives in MongoDB + the uploads volume; code updates never touch them. Made startup seed non-destructive — it NO LONGER overwrites an existing admin's password (a changed password now survives restarts/updates); only repairs role/permissions/active. `deploy/update.sh` is code-only (git pull + deps + build + restart) with explicit no-DB-ops guardrails.
- **Audit CSV export (2026-06):** `GET /api/audit/export.csv` (superadmin) + Export CSV buttons on the Audit Log page and the Dashboard System Activity widget.
- **Dashboard System Activity widget (2026-06):** superadmin-only recent audit feed on the Dashboard.
- **Audit log (2026-06):** `core/audit.py` `log_action` records every mutating action (login, create/update/delete/generate/share/password) across auth, users, clients, services, quotes, documents, files, settings into `audit_logs`. New `GET /api/audit` (SUPERADMIN ONLY, entity+action filters) and a new **Admin → Audit Log** page (superadmin nav). Verified 16/16 backend tests.
- **Live per-page SEO (2026-06):** `hooks/useSeo.js` (React 19, no dependency) renders `<title>`, description, keywords, Open Graph + Twitter Card meta on Home and Service pages. Home honors `page_seo` override for the home path; services use their meta_title/description/keywords/image.
- **Search-engine notifications (2026-06):** `core/seo_ping.py` + `POST /api/seo/ping` (seo perm) notifies Google/Bing (legacy, now deprecated → expected 404/410) and IndexNow (Bing/Yandex) when an `indexnow_key` is set; key file served at `GET /api/indexnow/{key}.txt`. Auto-pings on service/SEO changes when enabled + a canonical Site URL is set. Controls added to Admin → SEO Controls.
- **Backend modularized (2026-06):** Split the ~1400-line `server.py` into `core/` (db, config, utils, security, settings_store) + `routers/` (auth, users, services, settings, clients, portal, notifications, quotes, files, analytics, chat, documents, search). `server.py` is now an 87-line assembly file. Verified byte-behavior-identical: 28/28 regression tests + RBAC suites pass, no drift.
- **Production reverse proxy + SEO rewrites (2026-06):** Added `deploy/nginx.conf` (single-origin proxy that rewrites `/sitemap.xml`→`/api/sitemap.xml` and `/robots.txt`→`/api/robots.txt`), an optional commented `proxy` service in `docker-compose.yml`, and a new DEPLOYMENT.md section 6.
- **Larimar card image (2026-06):** Homepage "Larimar & Mineral Supply" service now uses the user-uploaded Larimar1.webp image.
- **Per-page SEO + sitemap.xml/robots.txt (2026-06):** Admin → SEO Controls now has a canonical Site URL, per-page meta overrides (path/title/description/keywords, add/remove), and one-click links to `/api/sitemap.xml` (auto-built from published services + overrides) and `/api/robots.txt`. NOTE: served under `/api/*` due to ingress; in production add a rewrite from `/sitemap.xml`→`/api/sitemap.xml` and `/robots.txt`→`/api/robots.txt`.
- **Client-linked documents (2026-06):** Documents already carry `client_id`; added `GET /api/clients/{id}/documents` (crm perm) and a Documents list inside the client edit modal (number, type, date, total, status + PDF download).
- **Brute-force login lockout (2026-06):** After N failed attempts an account locks for M minutes (HTTP 429). Configurable by admin in Settings → Login Security (defaults 5 attempts / 15 min). Counters reset on success. Verified via curl (3/1-min config → lock → 429 on correct password).
- **Renamed "Stytch" → "Stitch"** in Settings UI labels per user (backend keys unchanged). Social login still deferred pending user credentials.
- **RBAC fully enforced (2026-06):** All admin endpoints now use `require_perm(section)`. Completed the previously-open destructive/storage endpoints — `delete_service` (services), `update_quote`/`delete_quote` (crm), `upload_file`/`list_files`/`delete_file` (storage), `delete_document` (documents). Verified by backend testing agent (17/17 tests: superadmin allowed, sub-admins lacking the perm get 403, sub-admins with the perm allowed). `POST /api/quotes` remains intentionally public (intake form).
- **Approval alerts + social login connect:** Settings → Integrations now has Slack approval alerts (webhook + "alert on approval" toggle; fires a best-effort Slack message when a client approves a quote, run off the event loop) and a **Stytch** social-login connect option (project id + secret + enable toggle, saved for later). All secrets sanitized from the API.
- **Client Portal:** tokenized private link (no login) to view/download quotes/receipts, one-click **quote approval** → admin notification (bell + Dashboard Recent Activity), per-document share toggle, link expiry.
- **HS/tariff codes:** AI-suggested per line item; editable; on the PDF.
- **AI Refine/Regenerate:** inside the quote editor, an instruction box regenerates the draft (line items, totals, docs) via the AI.
- **Direct PDF download:** generated documents are downloadable straight from the document row (and the Send-to-Client folder).
- **Integrations (saved for later):** Settings → Integrations lets admin connect an email service (Resend/SendGrid) — provider, from-email, API key stored server-side (secrets never exposed via API). Actual email sending is NOT enabled yet.
- **AI Auto-Draft Quote:** describe a shipment → AI extracts line items + required documents; backend computes fees/customs/tax from Fee Calculator rules; draft opens pre-filled → save → one-click watermarked PDF.
- Public site: Hero, stats, dynamic services grid, dynamic service detail pages, about, contact.
- **AI Assistant (pluggable):** OpenAI/Anthropic/Gemini via emergentintegrations. Default Emergent key + admin BYO key in Settings. Public floating chat concierge (fees/docs/services + guides to quote form) and admin ops assistant (streaming).
- **Fee/customs calculator:** rule-based numbers (`/api/calculate`, rates editable in Settings) + AI explains required documents.
- **Quotes & Documents builder:** create quote/receipt/customs docs with line items + auto totals, auto number (EXD-Q-#####), generate watermarked (company logo) PDF → saved to "Send to Client" folder, downloadable.
- **Global search:** clients, quote requests, documents by name/phone/email/date/port/quote no./PO no./tracking no./item.
- Request-a-Quote pipeline (public form → CRM cards → convert to client).
- Visitor tracking + heatmap; CRM (Requests, Clients, Visitors, Documents); Services CRUD + per-service SEO; Object Storage; SEO controls; Settings; Profile.
- **Portability:** `storage.py` supports `STORAGE_BACKEND=local|emergent`; Dockerfiles, docker-compose.yml, .env.example files, and DEPLOYMENT.md for self-hosting outside Emergent.

## Architecture (updated)
- Backend modules: server.py (routes), ai.py (LLM), pdf_utils.py (PDF), storage.py (portable storage).
- AI streams as raw text/plain; frontend reads body stream (lib/chat.js).

## Test Credentials
admin@executivedistribution.com / Executive2025!

## Backlog / Next
- P1: Per-page (non-service) SEO overrides; sitemap.xml + robots.txt generation.
- P1: Client-linked documents in CRM (associate docs to a client).
- P2: Brute-force lockout on login; rate limit /api/track.
- P2: Drag-to-reorder services; multi-user admin roles.
- P2: Split server.py into routers as it grows.
